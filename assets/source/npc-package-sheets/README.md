# NPC PACKAGE SHEETS

These files were generated from `docs/art/NPC_READY_TO_COPY_PROMPTS.md`.

Purpose:
- Store approved NPC portrait and package-sheet source assets outside the runtime atlas flow.
- Keep blocked and missing prompt targets tracked separately.

Folder rules:
- `approved/`: approved NPC package sheets and ambient NPC set sheets
- `blocked/`: blocked outputs or placeholder notes for prompt targets that are missing or unusable

Current status:
- `approved/`: 19 files
- `blocked/`: 0 image files

Notes:
- For NPC prompts, the intended output is a portrait-plus-full-body NPC package sheet or an ambient NPC set sheet.
- Because of that, the current board-style layouts are acceptable source assets and do not need the transparency-conversion step used for equipment sheets.
- One prompt target is still missing: the weapon merchant of Lumen Village.
