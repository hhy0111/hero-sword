# VFX SHEETS

These files were generated from `docs/art/VFX_READY_TO_COPY_PROMPTS.md`.

Purpose:
- Store approved transparent VFX source sheets outside the runtime atlas flow.
- Keep original RGB board images archived for traceability.

Folder rules:
- `approved/`: transparent-background VFX source sheets approved for handoff
- `original-package-boards/`: archived original RGB board images used as conversion sources
- `blocked/`: blocked outputs when a prompt target is missing or unusable

Current status:
- `approved/`: 12 files
- `original-package-boards/`: 14 files
- `blocked/`: 0 files

Notes:
- All 12 prompts required transparent-background VFX sheets.
- The provided files arrived as RGB sheets with checkerboard baked in, so they were converted to actual RGBA transparent sheets before approval.
- The latest refresh batch had filename/content drift for the last three slots, so `desert / frost / ui` were re-assigned by actual image content before approval.
- The 2026-04-05 focused refresh batch replaced the dedicated `holy healing`, `water protection`, and `dark dash` source sheets with newer targeted exports.
- These are approved as source sheets, not runtime-ready particle atlases.
