- summary:
  - 2026-04-18 기준 스크린샷 재검토 후 `궁궐/마을 북측`, `스테이지 선택`, `전투`, `결과` 화면의 시각 문제를 코드 수정 가능 항목과 신규 아트 필요 항목으로 분리했다.
  - 즉시 품질을 깎는 자산은 런타임에서 배제하거나 약화하고, 최종 완성도는 신규 이미지 요청 배치로 넘기는 방향으로 정리했다.
- inputs:
  - 사용자 스크린샷:
    - 궁궐 북측 입구 흰 배경 노출
    - 마을 광장 표지판/프롭 깨짐
    - 스테이지 선택 화면 테스트용 레이아웃 노출
    - 전투/결과 화면 프레임과 텍스트 불일치
  - 참고 파일:
    - `src/game/scenes/VillageLobbyScene.ts`
    - `src/game/scenes/PalaceScene.ts`
    - `src/game/scenes/StageSelectScene.ts`
    - `src/game/scenes/BattleScene.ts`
    - `src/game/scenes/ResultScene.ts`
- decisions:
  - `KEEP`: 월드/전투/결과 흐름 구조
    - 이유: 기능 흐름은 이미 연결돼 있고 현재 문제는 주로 시각 자산과 레이아웃 품질이다.
  - `PATCH`: 마을 북측 궁궐 진입부
    - 이유: `outer_court_ground`의 흰 매트 배경이 화면을 직접 망가뜨려 즉시 비활성화가 필요하다.
  - `PATCH`: 궁궐 내부 홀
    - 이유: painterly 성격의 `throne_platform` 이미지가 게임 전체 도트 톤과 충돌하므로 코드 기반 대체 배경으로 임시 회귀한다.
  - `PATCH`: 스테이지 선택 레이아웃
    - 이유: 상단 패널 높이가 텍스트량을 수용하지 못해 테스트 화면처럼 겹침이 발생한다.
  - `PATCH`: 결과 화면 텍스트 패널 배치
    - 이유: 결과 프레임과 텍스트가 같은 좌표 체계를 쓰지 않아 시각적으로 분리돼 보인다.
  - `REPLACE`: 궁궐 외부/내부 최종 이미지, 스테이지 선택 최종 프레임, 전투/결과 최종 UI 프레임
    - 이유: 코드 보정만으로는 게임 톤과 최종 퀄리티를 맞출 수 없다.
- todo:
  - 궁궐 외부용 최종 이미지 제작 후 마을 북측에 재적용
  - 궁궐 접견실 배경/왕좌 연출 이미지를 최종 도트 아트로 교체
  - 스테이지 선택용 상단 프리뷰 프레임, 카드 프레임, 상태 배지 세트 적용
  - 월드맵 랜드마크 썸네일을 새 타워/요새 이미지로 교체
  - 전투 상단 HUD, 하단 커맨드 프레임, 결과 클리어/실패 프레임 교체
  - 왕궁 전용 런타임 NPC 시트와 초상 세트 추가
- risks:
  - 현재 전투/결과 화면은 코드 기반 임시 정리 상태라 최종 이미지가 없으면 시각 통일성이 제한적이다.
  - 스테이지 선택은 레이아웃은 개선 가능하지만 카드/배지 자산 없이는 완성형 RPG UI 느낌이 부족하다.
  - 궁궐 전용 NPC를 계속 기존 시트로 돌려쓰면 서사 허브의 고유성이 떨어진다.
- artifacts_changed:
  - `src/game/scenes/VillageLobbyScene.ts`
  - `src/game/scenes/PalaceScene.ts`
  - `src/game/scenes/StageSelectScene.ts`
  - `src/game/scenes/ResultScene.ts`
  - `docs/art/VISUAL_REWORK_READY_TO_COPY_PROMPTS_2026-04-18.md`
- handoff_to:
  - `asset_agent`
  - `ui_agent`
  - `qa_agent`
- handoff_notes:
  - 궁궐 북측과 내부는 현재 `문제 이미지 제거 + 코드 임시 배경` 상태다. 새 아트 배치가 오면 바로 재연결하면 된다.
  - 스테이지 선택은 상단 텍스트 겹침 문제를 코드로 먼저 잡았고, 최종 카드 프레임/배지는 새 이미지가 필요하다.
  - 전투/결과는 기능상 문제보다 프레임 자산과 텍스트 영역 조화가 핵심이라, 새 UI 프레임을 우선 제작하는 편이 효율적이다.
- done_check:
  - 문제를 `즉시 코드 수정 가능`과 `신규 이미지 필요`로 분리했다.
  - 다음 이미지 생산 배치가 무엇을 해결해야 하는지 문서와 프롬프트 기준이 생겼다.
